# Dependencies
node_modules/

# Working clone of the upstream parser, kept for looking up node types and for
# the next version bump. `script/clone-parser.js` in the editor puts it here.
.dev/

# Build artifacts
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.sublime-workspace

# Everything under spec/fixtures is content, whatever its extension. This
# package's own sample is a .log file, so `*.log` above had been silently
# keeping it out of the repository — `git add` said nothing, and CI opened a
# path that did not exist.
!spec/fixtures/**
